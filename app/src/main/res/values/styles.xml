<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/color_black_000000</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowNoTitle">true</item>
    </style>

    <style name="SearchText" parent="Widget.AppCompat.TextView">
        <item name="android:maxLength">20</item>
        <item name="android:singleLine">true</item>
        <item name="android:hint">@string/text_search_hint</item>
        <item name="android:ellipsize">end</item>
        <item name="android:background">@drawable/bg_rounded_search</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:fontFamily">@font/nanum_gothic</item>
    </style>

</resources>